<odoo>
    <record id="view_partner_form_inherit_products" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.products</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">

            <!-- Add client number before the name -->
            <field name="name" position="before">
                <field name="client_number"/>
            </field>

            <!-- Add new tab for Installed Products -->
            <notebook position="inside">
                <page string="Installed Products">
                    <field name="product_line_ids">
                        <form>
                            <group>
                                <field name="product_name"/>
                                <field name="place"/>
                                <field name="zone_number"/>
                            </group>
                        </form>
                    </field>
                </page>
            </notebook>

        </field>
    </record>
</odoo>
